# Stack: flutter  — (STUB — flesh out later)

- **detect:** `pubspec.yaml`; `lib/main.dart`; `.dart` sources; `ios/` + `android/` runner dirs.
- **structure:** _stub_ — `lib/` with feature/ui/data separation; `test/` mirrors `lib/`.
- **gitignore:** `build/`, `.dart_tool/`, `.flutter-plugins*`, `.packages`, `*.iml`, `.env*`,
  `.DS_Store`; platform build output under `ios/`/`android/`.
- **connectors:** _stub_.
- **cli_tools:** `gh`; the `flutter` / `dart` CLIs.
- **skills:** `/orchestrate`, `/handoff`.
- **claude_md_notes:** _stub_ — state management (Riverpod/Bloc/Provider), platform channels,
  target platforms.
- **first_command:** `flutter run` (or `flutter test`).
