# Stack: react-native  — (STUB — flesh out later)

- **detect:** `package.json` with `react-native` (or `expo`); `ios/` + `android/` dirs; a
  `metro.config.js`; `app.json` / `app.config.*` (Expo).
- **structure:** _stub_ — `src/` with screens / components / navigation / lib; note Expo vs bare.
- **gitignore:** `node_modules/`, `.expo/`, `ios/Pods/`, `ios/build/`, `android/build/`,
  `android/.gradle/`, `.env*`, `.DS_Store`.
- **connectors:** _stub_.
- **cli_tools:** `gh`; the package manager; `npx react-native` or the `expo` CLI; CocoaPods for
  bare iOS.
- **skills:** `/orchestrate`, `/handoff`.
- **claude_md_notes:** _stub_ — Expo vs bare workflow, navigation lib, native-module boundaries,
  which platforms build locally (iOS needs a Mac).
- **first_command:** `npx react-native start` (or `expo start`); `run-ios` / `run-android` to launch.
