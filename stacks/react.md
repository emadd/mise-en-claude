# Stack: react  — (STUB — flesh out later)

- **detect:** `package.json` with a `react` dependency; `.jsx` / `.tsx`; a Vite / CRA / Parcel config.
- **structure:** _stub_ — components / hooks / lib separation; colocate component tests.
- **gitignore:** `node_modules/`, `dist/`, `build/`, `.env*`, `coverage/`, `.DS_Store`.
- **connectors:** _stub_.
- **cli_tools:** `gh`; the package manager (`npm` / `pnpm` / `yarn`); the bundler CLI.
- **skills:** `/orchestrate`, `/handoff`.
- **claude_md_notes:** _stub_ — bundler, router, state-management approach, test runner.
- **first_command:** the project's dev script (commonly `npm run dev`).
