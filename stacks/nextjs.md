# Stack: nextjs  — (STUB — flesh out later)

- **detect:** `package.json` with `next`; `next.config.{js,ts,mjs}`; an `app/` or `pages/` dir.
- **structure:** _stub_ — App Router (`app/`) vs Pages Router (`pages/`); `components/`, `lib/`,
  server vs client component boundaries.
- **gitignore:** `node_modules/`, `.next/`, `out/`, `.env*`, `.vercel/`, `.DS_Store`.
- **connectors:** _stub_ — often a DB (Postgres/Prisma) and a deploy target (Vercel) worth
  wiring programmatically.
- **cli_tools:** `gh`; the package manager; `next` CLI; `vercel` CLI if deployed there.
- **skills:** `/orchestrate`, `/handoff`.
- **claude_md_notes:** _stub_ — router flavor, data-fetching model, server actions, env handling.
- **first_command:** `npm run dev` (Next dev server).
